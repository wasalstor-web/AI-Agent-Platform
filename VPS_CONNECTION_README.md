# دليل الاتصال بـ VPS Hostinger

## 📋 معلومات الاتصال

تم حفظ معلومات الاتصال بالخادم VPS Hostinger في الملفات التالية:

### الملفات المتاحة:

1. **`vps-connection-info.txt`** - ملف معلومات الاتصال الكاملة
   - ⚠️ **تحذير**: يحتوي على كلمة مرور حساسة
   - محمي في `.gitignore` ولن يتم رفعه إلى Git

2. **`connect-vps.sh`** - سكريبت تفاعلي للاتصال والتحكم
   - يوفر قائمة خيارات للاتصال والفحص
   - سهل الاستخدام

3. **`vps-status.sh`** - سكريبت فحص الحالة السريع
   - يعرض تقرير شامل عن حالة الخادم
   - مناسب للفحص السريع

## 🚀 الاستخدام السريع

### الاتصال المباشر:
```bash
ssh root@147.93.120.99
# كلمة المرور: 9'hG8lV1RCU)sesnQ3hA
```

### استخدام السكريبتات:

#### 1. الاتصال التفاعلي:
```bash
./connect-vps.sh
```

#### 2. فحص الحالة:
```bash
./vps-status.sh
```

### أوامر مباشرة مع sshpass:

```bash
# فحص الحالة
sshpass -p "9'hG8lV1RCU)sesnQ3hA" ssh root@147.93.120.99 "uptime && free -h && df -h"

# تنفيذ أمر مخصص
sshpass -p "9'hG8lV1RCU)sesnQ3hA" ssh root@147.93.120.99 "YOUR_COMMAND_HERE"
```

## 📊 معلومات الخادم

- **IP Address**: 147.93.120.99
- **User**: root
- **System**: Linux (cPanel)
- **RAM**: 15 GB
- **Disk**: 199 GB (190 GB متاح)
- **Services**: Apache, cPanel, Email, DNS

## 🔒 الأمان

- ملف `vps-connection-info.txt` محمي في `.gitignore`
- لا تشارك معلومات الاتصال مع أي شخص
- يُنصح بتغيير كلمة المرور بشكل دوري
- استخدم SSH keys بدلاً من كلمة المرور عند الإمكان

## 📝 ملاحظات

- تأكد من تثبيت `sshpass` قبل استخدام السكريبتات:
  ```bash
  sudo apt-get install -y sshpass
  ```

- جميع السكريبتات قابلة للتنفيذ (executable)
- يمكن تعديل السكريبتات حسب احتياجاتك
