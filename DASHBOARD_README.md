# لوحة التحكم الشاملة - AI Agent Platform Dashboard

## 📋 نظرة عامة

لوحة تحكم متعددة الصفحات لإدارة منصات AI والسيرفرات والمراقبة باستخدام Bootstrap 5.

## 📄 الصفحات المتوفرة

### 1. **dashboard.html** - الصفحة الرئيسية
- إحصائيات سريعة (السيرفرات، نماذج AI، استخدام الموارد)
- رسوم بيانية لأداء النظام
- حالة الخدمات
- النشاط الأخير
- إجراءات سريعة

### 2. **servers.html** - إدارة السيرفرات والمنصات
- **تبويب السيرفرات**: عرض وإدارة جميع السيرفرات
  - حالة الاتصال
  - استخدام الموارد (CPU, RAM, Disk)
  - إعادة التشغيل والإدارة
- **تبويب منصات AI**: إدارة نماذج AI
  - تصفية حسب النوع (عربي/عام/برمجة)
  - البحث عن النماذج
  - إحصائيات الطلبات والاستجابة
- **تبويب الخدمات**: إدارة الخدمات
  - Flask API Server
  - FastAPI DL+ System
  - OpenWebUI
  - Nginx, MySQL, Redis

### 3. **monitoring.html** - المراقبة والسجلات
- **تبويب الموارد**: مراقبة استخدام الموارد
  - CPU, RAM, Disk, Network
  - رسوم بيانية للنشاط
- **تبويب الأداء**: رسوم بيانية للأداء
  - أداء CPU والذاكرة
  - إحصائيات الطلبات
- **تبويب السجلات**: عرض سجلات النظام
  - تصفية حسب المستوى
  - البحث في السجلات
- **تبويب التقارير**: التقارير المتاحة
  - تقارير الأداء
  - تقارير الاستخدام
  - تقارير الأخطاء

## 🚀 كيفية الاستخدام

### 1. فتح لوحة التحكم
```bash
# افتح dashboard.html في المتصفح
open dashboard.html
# أو
xdg-open dashboard.html  # Linux
start dashboard.html      # Windows
```

### 2. التنقل بين الصفحات
- من الصفحة الرئيسية: استخدم الأزرار في الأعلى للانتقال
- جميع الصفحات تحتوي على رابط للعودة للصفحة الرئيسية

### 3. ربط APIs الفعلية
قم بتعديل متغير `API_BASE` في JavaScript:
```javascript
const API_BASE = {
    flask: 'http://localhost:5000/api',
    fastapi: 'http://localhost:8000/api'
};
```

## 🔧 الميزات

### ✅ تصميم متجاوب
- يعمل على جميع الأجهزة (Desktop, Tablet, Mobile)
- دعم كامل للغة العربية (RTL)

### ✅ تحديث تلقائي
- تحديث الإحصائيات كل 5 ثواني
- تحديث السجلات كل 5 ثواني
- تحديث الرسوم البيانية كل 3 ثواني

### ✅ واجهة مستخدم حديثة
- Bootstrap 5
- Chart.js للرسوم البيانية
- Bootstrap Icons
- تصميم Material Design

### ✅ جاهز للربط
- جميع الدوال جاهزة للربط بـ APIs الفعلية
- معالجة الأخطاء
- بيانات تجريبية للاختبار

## 📊 APIs المدعومة

### Flask API (Port 5000)
- `GET /api/health` - فحص الحالة
- `GET /api/status` - حالة API
- `GET /api/models` - قائمة النماذج
- `POST /api/process` - معالجة الأوامر

### FastAPI DL+ System (Port 8000)
- `GET /api/health` - فحص الحالة
- `GET /api/status` - حالة النظام
- `POST /api/process` - معالجة الطلبات

## 🎨 التخصيص

### تغيير الألوان
عدّل متغيرات CSS في `:root`:
```css
:root {
    --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    --success-gradient: linear-gradient(135deg, #10b981 0%, #059669 100%);
    /* ... */
}
```

### إضافة سيرفرات جديدة
عدّل مصفوفة `sampleServers` في `servers.html`:
```javascript
const sampleServers = [
    { id: 1, name: 'Server Name', ip: 'IP', status: 'online', ... },
    // ...
];
```

### إضافة نماذج AI جديدة
عدّل مصفوفة `sampleAIModels` في `servers.html`:
```javascript
const sampleAIModels = [
    { id: 'model-id', name: 'Model Name', provider: 'Provider', ... },
    // ...
];
```

## 🔒 الأمان

- لا يتم إرسال بيانات حساسة في الطلبات
- جميع الطلبات تستخدم HTTPS (في الإنتاج)
- معالجة آمنة للأخطاء

## 📝 ملاحظات

- البيانات الحالية هي بيانات تجريبية
- قم بربط APIs الفعلية للاستخدام الحقيقي
- يمكن تخصيص جميع الألوان والتصميمات

## 🐛 استكشاف الأخطاء

### المشكلة: لا تظهر البيانات
- تأكد من تشغيل APIs
- تحقق من عناوين URLs في `API_BASE`
- افتح Console في المتصفح للتحقق من الأخطاء

### المشكلة: الرسوم البيانية لا تعمل
- تأكد من تحميل Chart.js
- تحقق من وجود عنصر canvas في الصفحة

### المشكلة: التصميم لا يظهر بشكل صحيح
- تأكد من تحميل Bootstrap CSS
- تحقق من تحميل Google Fonts (Cairo)

## 📞 الدعم

للأسئلة والدعم:
- افتح Issue في GitHub
- راجع الوثائق في `README.md`

---

**صُنع بـ ❤️ للمجتمع العربي**

